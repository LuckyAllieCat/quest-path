You are generating ONE pixel-art game background with your image generation tool. The attached images are tiles from the SAME game (a coding-lessons path). Your tile must look like it belongs in the same set: same artist, same sprite sheet, same pixel size, same border, same outlines, same colours. Only the THEME PROPS change. Before drawing, study the attached tiles and copy these properties exactly:

- PIXEL SIZE (critical): low-resolution pixel art, effectively a 256x384 canvas scaled up 4x to 1024x1536. Every visible "pixel" is a 4x4 block. Nothing thinner than one block. No fine detail, no dithering, no texture noise, no soft shading, no painterly leaves. If a prop looks detailed or realistic, it is wrong: simplify it into a chunky cartoon shape.
- OUTLINES (critical): every prop, tree, bush, rock and the path has a thick dark outline (near-black brown or dark green, one block). Flat cel fills with only 2-3 shades per object. Canopies have a scalloped bumpy outline like the round trees in the attached tiles.
- SCALE OF PROPS: objects are BIG relative to the tile. A round tree canopy is about a quarter of the tile width (~240 px wide). A bush ~90 px. A rock cluster ~150 px. Draw fewer, larger, chunkier objects.
- GRASS: bright saturated flat green exactly like the attached tiles (#78b840 / #6cc048). NOT muted, NOT olive, NOT golden. Filled with small dark-green two-leaf tufts, small round outlined bushes and tiny 3-block white/yellow/pink flowers so there is little empty grass.
- PATH: sandy #e8c992, about 150 px wide, with a wavy dark-brown one-block outline and a slightly darker sand inner edge, exactly like the attached tiles. It MUST enter at the exact horizontal centre of the top edge and exit at the exact horizontal centre of the bottom edge, winding in wide S-curves between (tiles stack vertically). Use a different curve from the attached tiles.
- SIDE BORDERS (critical, copy exactly): on BOTH the left and right edges there is a wide border about 128 px wide in total: the outermost ~52 px is a flat dark navy-grey band (#36353f), then a ~72 px column of big chunky rounded brown cobblestones (#c88750 / #7d4a3f with dark outlines) with green ivy leaves and small pink flowers growing over them, then a dark outline where the grass starts. The border must be identical in style and width on both sides and run the full height.
- Portrait 1024x1536. NO user interface, NO text, NO signs, NO characters, NO lesson nodes, NO treasure chest.

Save the finished PNG into ./assets/ with the exact filename requested using python3 + PIL (convert to RGB, keep native 1024x1536, do not downscale). Print the saved path and PIL .size.
